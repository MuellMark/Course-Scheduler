Make sure to have python and flask installed
pip install Flask

If you need to reinstall enviroment but it should all be in the env folder
https://flask.palletsprojects.com/en/3.0.x/installation/

Commands: In Bash
cd src
source venv/bin/activate
python app.py

For powershell or on windows:
cd src
.venv2\bin\activate
python app.py